#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

typedef struct {
	int x;
    int x;
} pontos;

insertionsort, busca binaria; realoc

int main() {
    int i, x, y, grade, num_caixas;
    scanf("%d", &num_caixas);
    for(i = 1; i < num_caixas; i++){
        scanf("%d");
    }
    
	return 0;
}